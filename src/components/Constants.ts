export interface dataInterface {
    id?: string | number,
    name: string,
    destination: string,
    date: string | null,
    description: string,
    duration: number | null,
    riskAssessment: boolean
}